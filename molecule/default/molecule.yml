---
dependency:
  name: galaxy
  options:
    role-file: tests/requirements.yml
driver:
  name: docker
platforms:
  - name: ansible-role-elasticsearch-1604
    image: ubuntu:16.04

lint:
  name: yamllint
  enabled: False

provisioner:
  name: ansible
